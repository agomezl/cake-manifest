<?xml version="1.0" encoding="UTF-8"?>
<manifest>
    <!-- Remotes: name repository sources
    These can be relative paths, e.g. ../user_or_org ../agomezl
    or any other URI, e.g. https://android.googlesource.com/ -->
    <remote fetch="../CakeML/" pushurl="no_url" name="cake"/>
    <remote fetch="../polyml/"  name="poly"/>
    <remote fetch="../HOL-Theorem-Prover/" pushurl="no_url" name="hol"/>
    <default remote="cake"  revision="master"/>

    <project name="cakeml"               revision="master"/>
    <project name="hol"    remote="hol"  revision="master"/>
    <project name="polyml" remote="poly" revision="refs/tags/v5.7"/>
</manifest>
